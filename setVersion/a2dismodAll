#!/bin/bash
 
# Alle aktivierten PHP-Module im Apache finden und deaktivieren
for mod in /etc/apache2/mods-enabled/php*.load; do
    if [ -f "$mod" ]; then
        mod_name=$(basename "$mod" .load)
        sudo a2dismod "$mod_name"
    fi
done

echo "Alle aktiven PHP-Module wurden deaktiviert."